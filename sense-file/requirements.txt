# Extra dependencies for the sense-file tutorial.
#
# The Sense wheel has NO runtime dependencies -- install it separately for your
# interpreter/arch (built by the release pipeline):
#   pip install sense-<ver>-cp3XY-cp3XY-linux_<arch>.whl
#
# sense_file.py needs no extra packages beyond the Sense wheel.
